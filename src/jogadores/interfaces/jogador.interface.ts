export interface Jogador {
  readonly _id: string;
  readonly celular: string;
  readonly email: string;
  nome: string;
  ranking: string;
  posicaoRankin: number;
  urlFoto: string;
  categoria: string;
}
